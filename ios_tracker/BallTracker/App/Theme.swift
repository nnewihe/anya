import SwiftUI

/// Shared visual style for the tracker UI.
enum Theme {
    /// Tennis-ball yellow-green — the app's accent, used for the tracked
    /// trajectory, primary action buttons, and control tint.
    static let ballYellow = Color(red: 0.87, green: 1.0, blue: 0.16)
}
